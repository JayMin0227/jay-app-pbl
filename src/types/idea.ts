

// export interface Idea {
//   id: number;
//   title: string;
//   content: string;
//   tags: string[];
//   isCompleted?: boolean; // 完了状態のプロパティを追加
//   createdAt?: string;
//   updatedAt?: string;
// }

export interface Idea {
  id: number;
  title: string;
  content: string;
  tags: string[];
  isCompleted?: boolean; // 完了状態のプロパティを追加
  createdAt?: string;
  updatedAt?: string;
}
